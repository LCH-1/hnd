# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

HND의 주 사용자는 Claude Code, Codex, Cursor를 번갈아 쓰면서 여러 기기와 Git 저장소에서 같은 작업을 이어 가는 개발자다. 한 명의 운영자 또는 서로 신뢰하는 소규모 팀이 24시간 중앙 서버를 직접 운영한다. 제품에는 owner, admin, member 역할이 있고, 별도의 서버 운영자가 Docker, nginx, DNS, TLS와 백업을 관리한다.

## Product Purpose

HND는 코딩 에이전트용 룰, 검증 가능한 작업 진행 상태, 사람이 보강한 인수인계, 오래 남길 지식을 한 흐름으로 관리한다. 에이전트나 PC를 바꿔도 이미 정한 원칙과 작업 맥락을 반복해서 설명하지 않고 이어 가는 것이 성공 기준이다.

## Positioning

일반 메모 도구와 달리 HND는 저장소와 실행 환경을 식별하고, 룰을 정해진 우선순위로 합성해 각 에이전트가 실제로 읽는 로컬 형식으로 전달한다. Git 상태 기반 자동 체크포인트와 선택적으로 작성하는 작업 판단·장기 지식을 분리해, 자동화의 편의와 사람이 남겨야 하는 맥락을 함께 보존한다.

## Operating Context

- 운영자는 Docker Compose로 중앙 서버를 띄우고 외부 nginx 또는 ingress에서 HTTPS를 종료한다.
- 사용자는 웹에서 패스키로 로그인하고, 최초에는 소유자 계정·복구·서버 보관함·PC 연결을 단계별로 설정한다. 첫 PC와 추가 PC 모두 로그인한 계정에서 같은 방식으로 일회용 연결 코드를 만든다. 기존 PC는 필요하지 않다.
- 각 개발 PC에는 공개 패키지 `@lch-1/hnd@0.2.2`로 고정 launcher를 한 번 설치한다. 이후 connector runtime은 인증된 중앙 서버에서 서명본을 자동으로 받고, 사용자 범위 에이전트 훅을 설치한다. Git 저장소는 AI 세션을 처음 시작할 때 자동 등록한다.
- 세션 시작, 응답 종료, 세션 종료 훅이 짧게 동기화를 시도한다. 서버나 네트워크를 사용할 수 없으면 마지막으로 검증된 로컬 캐시로 계속 작업하고 다음 훅에서 재시도한다.

## Capabilities and Constraints

- 룰은 전체, 저장소, 현재 Git 체크아웃에 선택한 환경, 동기화하지 않는 이 PC 전용 override 순서로 합성한다. 같은 저장소라도 배포·테스트 체크아웃은 서로 다른 환경을 선택할 수 있다. repo-subpath 룰은 현재 범위 밖이다.
- Stop과 SessionEnd에서 브랜치, HEAD, 마지막 commit, 변경 경로를 자동 체크포인트로 기록한다. 대화 전문이나 소스 내용은 자동 수집하지 않는다.
- 작업 인계에는 목표, 현재 상태, 결정, 제외한 접근, 변경 파일, 검증, 다음 작업, 열린 질문을 사용자가 필요할 때 보강할 수 있다.
- 유효 룰, 선택한 활성 작업과 자동 체크포인트는 하나의 revision된 Live Context로 합성한다. 실행 중 세션은 다음 입력 전에 변경된 최신 snapshot만 받고, 새 세션과 resume·clear·compact는 최신 revision을 강제로 확인한다. Cursor의 관리 파일은 누적하지 않고 교체한다.
- 장기 지식은 사용자가 명시적으로 저장한 제목·태그·본문만 공통, 프로젝트, 프로젝트 환경 범위로 보관하고 검색한다.
- 중앙 서버는 계정·기기·초대·리비전, 보호된 tenant vault key와 암호화 snapshot을 SQLite에 저장한다. 패스키 인증과 데이터 암호화 키는 분리된다.
- 서버는 raw 32-byte, mode `0600`인 `/data/server-vault.key`로 tenant vault key를 보호하고 필요하면 snapshot을 복호화할 수 있다. 따라서 HND는 E2EE나 zero-knowledge를 표방하지 않으며, SQLite와 master key 파일을 모두 백업해야 한다.
- PC 연결 권한의 기준은 최초 등록 PC가 아니라 로그인한 서버 계정이다. 계정이 웹의 **기기 → PC 연결**에서 코드를 만들고 `hnd connect`가 소비한다. `hnd sync invite`는 더 이상 코드를 만들지 않는다.
- 새 브라우저와 PC는 인증된 계정에서 같은 보관함을 받는다. 로컬 vault key는 오프라인 캐시를 열기 위한 사본이지 새 기기 승인의 기준이 아니다. 기존 E2EE key export/import는 이전 설치 전환 보조 도구로만 유지한다.
- 신규 가입은 기본적으로 초대 없이 열려 있고 각 계정은 분리된 작업 공간을 받는다. 같은 작업 공간에 합류시키는 초대와 서버 전체의 공개 가입 정책은 별도로 관리한다.
- 중앙 서버는 등록된 PC에만 Ed25519 서명된 connector runtime을 제공한다. launcher는 서명·digest·단조 sequence를 확인한 뒤 원자적으로 전환하고, 실패하거나 서버가 꺼져 있으면 마지막 정상 runtime 또는 내장본으로 돌아간다.
- 마지막 동기화 기준본·로컬본·서버본을 비교해 서로 다른 룰·프로젝트·작업·지식 항목은 자동 병합한다. 같은 항목의 동시 변경, 인증 실패, 암호문 무결성 실패는 자동 덮어쓰기하지 않고 attention 상태로 멈춘다.
- 에이전트에 전달하는 합성 문서는 기본 32 KiB를 넘으면 중간을 자르지 않고 전체 합성을 거부한다.
- 실시간 push나 상시 background daemon은 없다. 다른 기기의 변경은 보통 다음 세션 훅에서 도착하며, connector 업데이트도 실행 시점의 짧은 background 확인으로 가져온다.
- 원격 snapshot은 암호화되지만 서버가 master key로 복호화할 수 있다. 오프라인 작업용 로컬 HND 파일은 같은 OS 사용자에게 보이는 평문 private 파일이며, device-at-rest 보호는 OS 디스크 암호화에 맡긴다.
- `/`은 로그인 또는 최초 설정 입구, `/setup`은 단계별 설정, `/app`은 프로젝트·룰·작업·지식·기기·저장 기록·보안·설정 작업 공간이다. 프로젝트 화면은 Git 저장소의 자동 등록 상태, 프로젝트·환경 룰, 진행 작업과 다음 설정 단계를 한곳에서 보여준다.
- 현재 배포 모델은 단일 프로세스·로컬 volume 기반의 소규모 self-host다. HA, 결제, 공개 SaaS 운영, 조직 승인 workflow, SSO는 제공하지 않는다.
- 기존 route, DOM id, `data-*`, form name, ARIA 연결, offline cache와 보안 헤더 계약을 시각 개편에서도 유지한다.

## Brand Commitments

- 제품명은 `HND`다.
- 첫 화면은 홍보용 랜딩 페이지가 아니라 로그인 또는 최초 설정을 위한 설치된 서버의 입구다.
- 웹은 브라우저 언어, CLI는 OS 언어를 기본으로 한국어와 영어를 제공한다. 사용자가 저장된 설정으로 바꿀 수 있고, 두 언어 모두 짧고 기능적으로 쓴다. CLI의 긴 명령에는 짧은 별칭을 제공한다.
- 시각 언어는 세련되고 차분한 현대적 제품 UI여야 하며, 전형적인 AI SaaS 템플릿처럼 보이면 안 된다.
- Immich는 무채색 면적, 제한된 강조색, 콘텐츠 중심 밀도, 단일 작업형 로그인·설정 흐름의 품질 기준이다. Immich의 로고, 정확한 색, 글꼴, 사진 서비스 정보구조를 복제하지 않는다.

## Evidence on Hand

- 요구사항 기준선: `PLAN.md`
- 현재 구현 결정과 위협 모델: `docs/ARCHITECTURE.md`
- 실제 제품 동작과 사용법: `README.md`, `src/web/`, `src/server/`, `src/sync/`
- 자동화된 계약 테스트: `test/`
- 실제 고객 수, 성능 벤치마크, 추천사, 상업적 지표는 제공되지 않았다. 향후 화면에서도 이를 만들어 내지 않는다.

## Product Principles

1. 정상 사용은 자동으로 처리하고, 진단·충돌 해결·맥락 보강에는 명시적 수동 제어를 남긴다.
2. 중앙 서버가 없어도 작업을 막지 않으며, 복구된 연결에서 안전하게 수렴한다.
3. 서버 계정을 연결 권한의 기준으로 삼고 인증과 암호화의 역할을 분리하되, 서버가 복호화 가능한 신뢰 모델과 master key 백업 책임을 숨기지 않는다.
4. 사용자의 기존 저장소와 에이전트 설정을 보존하고 HND가 소유한 부분만 변경하거나 제거한다.
5. 에이전트가 추측하지 않도록 룰, 자동 체크포인트, 인간의 판단, 장기 지식을 서로 다른 기록으로 다룬다.

## Accessibility & Inclusion

한국어 문서 구조, 키보드 전용 사용, 명확한 focus 상태, 네이티브 dialog·details·label 의미론, 최소 44px 터치 대상, 320px reflow, 200% 확대, WCAG AA 대비와 `prefers-reduced-motion`을 유지한다. 상태는 색만으로 전달하지 않는다.
