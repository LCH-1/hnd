> **문서 지위:** 이 파일은 요구사항과 조사 결과를 보존하는 기준선(requirements/research baseline)이다.
> 아래의 설계·일정은 조사 당시 제안이며 현재 구현 명세가 아니다.
> 구현 과정에서 확정한 판단과 PLAN 대비 변경은 [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md)에 기록한다.
> 내용이 충돌하면 실제 동작은 코드·테스트와 해당 ADR을 기준으로 판단한다.

# handoff — 요청 · 결론 · 작업 계획

최종 갱신 2026-08-25 · 조사 4회(에이전트 33개, 웹조회 1,700여 회) 결과 반영

이 문서는 **새 세션이 처음부터 다시 조사하지 않도록** 하는 것이 목적이다.
"확정된 사실"은 재검증 없이 신뢰해도 된다. 대부분 이 PC에서 직접 실행해 확인한 것이다.

---

## 1. 무엇을 요청했나 (요구가 바뀐 순서 그대로)

요구는 네 번 확장됐다. 순서를 남기는 이유는, 설계가 왜 바뀌었는지를 다시 논쟁하지 않기 위해서다.

| 시점 | 요청 |
|---|---|
| 최초 | Cursor·Codex·Claude Code를 섞어 쓰는데 룰이 제각각이고 세션이 공유가 안 된다. 최소 기능 셋 — ① 모든 에이전트 공통 **전역 룰** ② **프로젝트별 룰** ③ **프로젝트 세션 주요사항 저장**. 기존 서비스가 맞으면 그걸 써도 좋다 |
| 2차 | 진짜 목표는 **"Claude로 하다가 Cursor로, Codex로 섞어 써도 세션이 안 끊긴 느낌"** |
| 3차 | 구축은 오래 걸려도 되니 **사용이 간편할 것**. 처음에 빡세게 하고 나중에 프로젝트별로 손쉽게 |
| 4차 | 프로젝트 구분을 **로컬 경로가 아니라 저장소(GitHub repo) 기준**으로. 그리고 **repo 룰**과 **배포 환경별 repo-local 룰**을 따로 |
| 5차 (방향 전환) | **100명이 쓰고 싶어 하면 어떻게 배포하나.** git clone·npm 각자 관리는 100명 기준으로 마찰이 크다 → **서버 하나 두고 DB에 계정, user auth로 그 계정 룰을 받아 쓰는 방식**. URL 기반이라 GitHub/GitLab 계정과 무관. **호스팅은 내가 운영하되 Docker로 self-host 선택지 제공** |

**범위 밖으로 확정된 것:** 남의(내가 소유하지 않은) repo에 룰을 거는 것.

---

## 2. 확정된 사실 (재조사 불필요)

### 2.1 에이전트 로딩 — 이 설계의 토대

- **에이전트는 파일만 읽는다. URL을 읽는 에이전트는 하나도 없다.** 전송 방식과 무관하게 바이트는 로컬 디스크에 떨어져야 한다. 서버는 "로컬 파일 단계"를 없애주지 못하고 가져오는 방법만 바꾼다.
- **지시문 로더는 git을 조회하지 않는다. 파일시스템을 훑을 뿐이다.** `.git/info/exclude`로 전부 무시시켜 `git status --porcelain`이 완전히 빈 상태에서 `claude -p`와 `codex exec`를 돌렸더니 모든 지시문 파일이 정상 로드됐다. → **"커밋하지 않기"의 비용이 0이다.** (Claude Code 2.1.243 / Codex 0.149.1 실측)
- **READ는 파일, 훅이 아니다.** 파일 로딩은 승인 0회 클라이언트 보장. 훅 기반 주입은 에이전트별·머신별 신뢰 다이얼로그를 요구한다.
- **MCP는 자동 주입 채널로 사망.** pull 전용이고 `instructions` 핸드셰이크 필드가 스펙 2026-07-28에서 제거(SEP-2575).

### 2.2 에이전트별 특성

| | 사실 |
|---|---|
| Claude Code | `~/.claude/rules/*.md`(paths 키 없음)는 매 세션 무조건 로드. `CLAUDE.local.md`도 무조건 로드되며 gitignore돼 있어도 로드됨. `@path` import는 4홉까지 재귀 |
| Codex | 전역에서 **첫 번째 비어있지 않은 파일 하나만** 읽는다. 디렉터리 스캔도 include도 없음. `AGENTS.override.md`가 있으면 `AGENTS.md`를 **아예 안 읽는다**(대체지 추가 아님). `project_doc_max_bytes` 기본 32 KiB에서 **조용히** 잘라냄 |
| Cursor | `.mdc`만 읽고 `.md`는 **오류 없이 무시**. `alwaysApply: true` 필요. 복수 always 룰의 **순서는 정의돼 있지 않음** → 파일 하나로 몰 것. sessionStart `additional_context` 주입은 스태프 인정 버그(훅은 실행되고 로그도 성공, 주입만 안 됨) |

### 2.3 포인터 방식이 불가능한 이유 (전부 실측)

- 프로젝트 파일의 `@~/.handoff/...` 외부 import는 **헤드리스 `claude -p`에서 조용히 버려진다.** 다이얼로그도 에러도 없음. 승인 상태는 `~/.claude.json`의 **절대 경로 키**로 저장돼서 머신마다·체크아웃마다 다시 받아야 한다 — 벗어나려는 그 축이다.
- **user 스코프에서 특정 repo를 겨냥할 수 없다.** `paths:` 글롭이 **프로젝트 상대 경로**에 매칭된다. `**/scratchpad/proj/**`는 안 걸리고 `sub/**`는 걸린다.
- **Codex `[projects."<path>"]`는 필드가 `trust_level` 하나뿐이다** (바이너리 확인).
- **심볼릭 링크는 이 PC에서 못 쓴다.** PowerShell은 관리자 권한 요구, **Git Bash `ln -s`는 성공했다고 하면서 37바이트 일반 파일을 만든다** — 맞아 보이는데 조용히 낡는 최악의 실패.

### 2.4 저장소 식별자

```
K1  root:<40hex>   git rev-list --max-parents=0 --first-parent origin/HEAD
K3  url:<정규화>    shallow clone 등 K1 불가 시 폴백
```

- `--first-parent`는 **필수**. 없으면 git.git 같은 저장소에서 루트가 **7개** 나온다(병합된 하위 프로젝트들이 자기 루트를 가짐).
- root-commit SHA는 clone 두 개에서 동일함을 실측했고 rename·transfer·re-hosting·fork를 견딘다.
- `HEAD`가 아니라 `origin/HEAD`에서 뽑아야 orphan 브랜치 체크아웃이 정체성을 바꾸지 않는다.

### 2.5 보안

- **위협 방향은 "내가 남의 룰을 읽는 것"이 압도적.** Mitiga가 2026-04~06에 공개 repo 7,000곳의 지시문 파일 5만 개를 스캔해 실제 유출 페이로드를 발견(Webhook.site·Pipedream·Supabase·Telegram으로 프롬프트·환경변수·AWS/GitHub 토큰). 같은 파일들에서 하드코딩 키 1,230개 이상.
- **단 멀티테넌트에서는 방향이 뒤집힌다.** 격리 버그 하나면 남이 내 룰에 쓰는 게 된다. Asana MCP 교차 노출(2025-06, 약 1,000개 고객).
- **실제로 강제되는 통제:** Codex `network_access=false`(기본값), Claude `permissions.deny`(bypass 모드에서도 작동), PreToolUse 훅 exit 2. **룰 파일에 "보내지 마"라고 쓰는 건 무의미** — 벤더 문서가 "컨텍스트이지 강제 설정이 아니다"라고 명시.
- `disableAllHooks`는 신뢰할 만한 킬스위치가 **아니다**(프로젝트 설정이 되돌릴 수 있음). 진짜는 `claude --bare` / `--setting-sources user`.

### 2.6 이 PC의 사실

- node v24.18.0 (`C:\Program Files\nodejs\node.exe`), git 2.49.0.windows.1
- **PATH의 `bash`가 WSL이다.** 에이전트 훅에 `shell: bash` 금지, node를 셸 없이 쓸 것. (git 훅은 예외 — Git for Windows가 자체 MSYS sh를 씀)
- Codex `notify` 슬롯은 `codex-computer-use.exe`가 점유 중
- `cursor-agent` 미설치
- **`~/.codex/config.toml`에 평문 `SENTRY_ACCESS_TOKEN` 있음 → 로테이션 필요**
- **`%ProgramData%\OpenAI\Codex`가 관리자 권한 없이 쓰기 가능** → 정책 신뢰 훅을 심을 수 있는 구멍. ACL 조일 것

### 2.7 기각된 선택지

| 대상 | 기각 사유 |
|---|---|
| 호스팅 메모리 제품 18종 | **룰을 반드시 주입되는 일급 개념으로 저장하는 제품이 0개.** 메모리로 저장된 룰 = 에이전트가 조용히 건너뛸 수 있는 룰 |
| 플러그인 마켓플레이스 | `codex features list` → `plugin_hooks removed false`. Codex 플러그인은 훅을 못 실음. Claude 플러그인 훅은 워크스페이스 신뢰를 요구하는데 `~/.claude/settings.json` 훅은 안 요구 |
| chezmoi | 미설치, 그리고 13개 타겟 중 11개가 통째로 새로 만드는 파일이라 남는 일이 거의 없음 |
| `git init.templateDir` | `$GIT_DIR`이 `.git/`이라 워킹 트리에 파일을 못 놓음. `core.hooksPath`와 상호 배타 |
| npx 핫패스 / 컴파일 바이너리 / direnv | 각각 레지스트리 재해석·서명 없는 배포·디렉터리마다 `direnv allow` |
| Merkle 투명성 로그 | 서버가 서명한 로그를 그 서버를 믿는 클라이언트가 검증 = 이 규모에선 연극. 가장 어려운 2주치 코드 |
| Postgres + RLS | 300대 × 40회/일 = **0.14 rps**. 테넌트당 SQLite 파일이 더 단순하고 **더 강한 격리**(교차 읽기를 표현할 코드 경로가 없음) |
| block/buzz | Nostr 릴레이 팀 워크스페이스. 카테고리 다름. 단 Block도 자기 룰은 `CLAUDE.md`→`AGENTS.md` 심볼릭 링크로 해결 |
| rootnix/mindpalace | 설계는 (c)에 가장 잘 맞지만 14커밋·4스타·다운로드 1회. `.mindpalace/`를 신뢰 검사 없이 주입하고, `mp init -g`가 남의 설정 7개를 고치고, 서명 없이 자가 업데이트 |

---

## 3. 설계 결론

### 3.1 층 모델

```
10 GLOBAL       개인 취향. 모든 프로젝트, 모든 머신
20 REPO         저장소 정체성 기준. 모든 머신에서 동일
25 REPO-SUBPATH 모노레포 하위 경로 (longest-prefix)
30 REPO-LOCAL   저장소 × 환경 (laptop / prod-seoul ...)
35 SESSION      프로젝트 세션 핸드오프
40 GUARD        클라이언트 전용. 서버가 보낸 모든 것 뒤에 붙고 절대 업로드 안 됨
```

**충돌 시 숫자가 큰 층이 이긴다**를 각 블록 헤더에 명시한다. 세 에이전트의 순서 규칙이 서로 다르고 Cursor는 문서화도 안 돼 있어서, **파일 순서에 기대지 않고 한 파일 안에 hnd가 정한 순서로 이어붙인다.**

### 3.2 전달 타겟

| 층 | 위치 | 방식 |
|---|---|---|
| 10 (머신 레벨) | `~/.claude/rules/40-handoff.md` | 통째 소유 |
| | `~/.codex/AGENTS.md` | **마커 블록** (아래 참조) |
| | `~/.agents/skills/handoff/SKILL.md` | 통째 소유 |
| | `~/.cursor/plugins/local/handoff/rules/00-handoff.mdc` | 통째 소유 |
| 20/25/30 (repo 레벨) | `<worktree>/.claude/rules/50-hnd.md` | 통째 소유 + gitignore |
| | `<worktree>/.cursor/rules/50-hnd.mdc` | 통째 소유 + gitignore |
| | `<worktree>/AGENTS.hnd.md` + `project_doc_fallback_filenames` | 통째 소유 + gitignore |

**`~/.codex/AGENTS.md`만 마커 방식인 이유:** Codex는 전역에서 파일 하나만 읽으므로 네임스페이스를 만들 수 없고 사용자 파일을 공유해야 한다. 통째 소유로 두면 **이미 그 파일을 쓰던 사람의 첫 실행이 실패**한다.

### 3.3 신뢰 모델 — 평문 서버로 결정

E2E 암호화는 **웹 UI · 서버측 합성 · 복구**와 상호 배타적이다. 셋 중 둘만 가질 수 있고, 선택한 조합이 평문을 강제한다.

서버측 합성은 편의가 아니라 **정확성 요구사항**이기도 하다 — Codex의 32 KiB 한도 안에서 **층 경계로** 잘라내려면 전체 바이트 수를 아는 쪽이 서버뿐이다. 클라이언트에 맡기면 룰 중간에서 잘린다.

**침해 시 대가 (명시):** 100명 ≈ 300대. 침해된 서버는 그 300대의 룰 파일에 임의 지시문을 쓴다. 그걸 읽는 에이전트는 git 자격증명·클라우드 CLI·소스 트리를 쥐고 있다. **이 구조 안에 암호학적 완화책은 없다** — 서버가 렌더링한 내용에 서버 키로 서명해도 침해된 건 그 서버다. 서명은 신뢰를 옮기지 못하고 키만 옮긴다.

**대신 실질 방어 두 개:**
1. **프로토콜은 마크다운 산문만 나른다.** 파일 경로·훅·`allowedTools`·MCP 서버 지정 불가. frontmatter는 닫힌 허용목록(`name`/`description`, `alwaysApply`/`description`)으로 검증하고 나머지는 버리고 보고한다. → **침해가 사는 건 프롬프트 텍스트지 설정이 아니다.**
2. **`~/.hnd/guard.md` = 층 40.** 클라이언트 전용, 서버 콘텐츠 **뒤에** 붙고, 절대 업로드 안 됨. **서버는 이걸 제거할 수 없다.**

### 3.4 서버

- **Go 단일 바이너리 + 테넌트당 SQLite 파일.** distroless, ~18 MB, non-root, amd64+arm64
- 격리 = **파일 분리**. 교차 읽기를 표현할 수 있는 코드 경로가 없다
- 테넌트는 **토큰에서만 파생**. 요청 본문의 id를 절대 믿지 않는다
- 등록은 Tailscale auth key 방식 — `hnd enroll --key hnd_...` 한 줄. 브라우저 불필요
- ETag + `If-Match`로 낙관적 동시성. 412를 클라이언트가 CONFLICT로 보고
- 설정은 env var만(`HND__section__KEY`), `BASE_URL` 불일치 시 **부팅 거부**
- 배포 quickstart는 **다이제스트 핀**. `:latest`는 매 pull이 무검토 업그레이드가 된다

---

## 4. 지금까지 만든 것 (동작함)

```
~/.handoff/src/          도구 (git repo, 커밋 3개)
  bin/hnd.mjs              sync · doctor · sync --uninstall, 전부 --dry-run
  lib/paths.mjs            경로 통일 (Windows는 USERPROFILE 우선, 슬래시 정규화)
  lib/ledger.mjs           장부 — sha 기록, 깨지면 "아무것도 소유 안 함"으로 안전 실패
  lib/sole.mjs             통째 쓰기 + 원자적 tmp+rename + 백업
  lib/marker.mjs           마커 블록 — 바깥 바이트 보존
  lib/render.mjs           원본 하나 → 에이전트별 포장
  lib/git.mjs              git 래퍼 (인자 배열, 셸 안 거침)
  rules/10-handoff.md      인수인계 규약
  test/marker.test.mjs     계약 15개, 전부 통과
~/.handoff/rules/        내 콘텐츠 (별도 repo, remote 미설정)
```

**검증된 것:** dry-run이 정말 안 씀 · 4타겟 생성 · 멱등 · **손댄 파일 안 덮고 내용 보존** · 룰 한 줄이 4곳 전파 · Cursor `.mdc` frontmatter · uninstall이 자기 것만 제거 · **마커 블록 바깥 편집은 충돌 아님** · **제거 후 사용자 내용 바이트 동일** · 32 KiB 초과 시 거부

**남은 결함:** 콘솔 출력과 프로토콜 문안이 한국어 → 배포하려면 출력은 영어로 분리 필요

---

## 5. 작업 예정

### 0단계 — 클라이언트 리팩터 (1.5주) · **서버 없이 오늘 가치 있음**

**어느 경로로 가든 필요하고 버려지지 않는다.** 여기서 멈춰도 지금보다 나은 개인 도구가 남는다.

- 합성을 **소스 인터페이스**로 리팩터 — `file://` 구현, `https://` 스텁
- 층 모델 · 우선순위 서문 · 층별 마커 · frontmatter 검증을 클라이언트로
- `hnd merge` — CONFLICT에 갇힌 사용자를 꺼냄
- `hnd preview` — 네 타겟에 들어갈 **정확한 바이트**를 디스크에 안 쓰고 출력. 디버깅 도구이자 보안 검토 도구
- 콘솔 출력 영어화 + 룰 콘텐츠는 사용자 언어 유지

> `file://` 소스는 **영구 지원 기능**이지 임시 발판이 아니다. 오프라인 스토리이자 서버 장애 시 폴백이며, 오늘 동작하는 코드를 계속 살려두는 장치다.

### 이후 (서버로 갈 경우)

| 단계 | 내용 | 기간 | 멈춰도 남는 것 |
|---|---|---|---|
| 1 | 서버 코어: enroll · index · bundle (global만, UI 없음) | 2.5주 | 키 하나 붙여넣으면 새 헤드리스 SSH 박스에 내 룰이 올라감 |
| 2 | 웹 UI (global) + 리비전 히스토리 | 2주 | CLI를 안 쓸 사람도 쓸 수 있음. "왜 갑자기 이상해졌지"에 답이 됨 |
| 3 | 저장소 정체성 + repo 스코프 + project 티어 | 3주 | 같은 프로젝트, 같은 룰, 어느 머신·어느 경로에서든 |
| 4 | 환경 + repo-local + `/v1/preview` | 1.5주 | 노트북 vs prod 박스 문제 해결 |
| 5 | 오프라인 강화 | 1.5주 | 100대에서 무인 실행해도 안전해짐 |
| 6 | 세션 상태 = 층 35 | 1.5주 | **에이전트 간 인수인계** — 세 에이전트 중 어느 것도 혼자 못 하는 것 |
| 7 | self-host 릴리스 | 2주 | 누구나 돌릴 수 있음. 호스팅이 단일 정책 지점이 아니게 됨 |
| 8 | pin · 변경 게이팅 · 폐기 캐스케이드 | 1주 | prod 환경에 자동 업데이트 opt-out |

**격리 경계는 1단계에 넣는다. 나중으로 미루지 않는다.** 동작하는 단일 테넌트 서버에 격리를 나중에 얹는 게 Asana 사고가 난 방식이다.

### 기간 — 정직하게

| | 기간 |
|---|---|
| 개인 도구로 완성 | **~2주** |
| 서비스로 완성 | **~20주 풀타임** (저녁·주말 10h/주면 12~18개월) |

20주는 설계 문서에 안 적히는 것들(가입·비밀번호 재설정·계정 삭제/내보내기·메일 발송·에러 페이지·레이트 리밋·3개 OS CI·패키징·문서 사이트, 그리고 어느 에이전트의 로더가 문서와 다르게 동작해서 사라지는 2~3주)을 **30% 더해서** 나온 숫자다.

추가 18주가 사는 것은 넷뿐이다: ① git repo·계정 없는 사용자 온보딩 ② 웹 UI ③ rename을 견디는 저장소 기준 룰 ④ 남에게 건넬 Docker 이미지.

---

## 6. 아직 안 정한 것

- **20주를 보고 시작할 것인가** ← 가장 큰 미결
- 웹 UI 범위 (룰 편집을 브라우저에서 어디까지)
- 무료/유료 — 100명이면 호스팅 비용이 실제로 발생
- 클라이언트 배포 채널 — 공개 npm 패키지 `@lch-1/hnd`로 통일한다. 패키지는 connector 파일만 포함하고 lifecycle `install`/`postinstall` 동작으로 저장소나 에이전트 설정을 바꾸지 않는다. 중앙 서버는 별도 설치 파일을 제공하지 않는다.

---

## 7. 못 하는 것 (약속하면 안 되는 것)

- **진짜 작업 중간 인수인계.** 열린 파일·반쯤 쓴 함수·툴 상태는 안 넘어간다. "작업 단위 사이"가 천장. 연속성 체감의 80%는 **'버린 접근' 섹션**에서 나온다 — 새 에이전트가 남처럼 느껴지는 건 열린 파일을 몰라서가 아니라 이미 배제한 방법을 다시 제안해서다
- **웹 UI를 가지면서 침해된 서버로부터 암호학적으로 보호받기.** 연기가 아니라 영구히 불가능
- E2EE + 복구 + 웹 UI 셋 다. 둘까지만
- 실시간 전파. 룰은 다음 sync에 도착하고 **폐기된 기기도 그때까지 룰을 갖고 있다**
- 커밋 없이 clone을 따라가는 룰. hnd가 안 깔린 머신에는 안 간다
- Codex Cloud / Claude Code 클라우드 런에는 안 간다
- 인자 없는 첫 `hnd init`은 중립적인 `default` 환경을 선택한다. `prod`, `test`처럼 역할이 있는 환경은 각 PC에서 `hnd env set <라벨>`로 명시해야 한다. 호스트명으로 역할을 추측하지 않는다.
- 세 에이전트의 동일한 룰 용량. Codex 32 KiB는 나머지 둘에 없는 천장

---

## 8. 참고

- 전체 보고서(구축 명세서 + 사용법): https://claude.ai/code/artifact/71dfad81-b602-496a-a486-c058c09d4861
- 훔쳐온 것: Tailscale(auth key 등록, ETag/If-Match, `/preview`) · Bitwarden(신뢰 모델 검토) · conda(마커 블록) · EditorConfig(`root=true` 스톱 마커) · Renovate(저장소 기준 공유 설정) · Block buzz(core/cold 메모리 분리, `base_prompt.md`) · mindpalace(정확한 문자열 치환 편집 규율)
